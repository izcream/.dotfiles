# personal .zshrc and starship config

### Install

- add `source $HOME:/.dotfiles/.zshrc` to `~/.zshrc` file in `$HOME` folder
- run `ln -s $HOME:/.dotfiles/starship.toml ~/.config/starship.toml` for link starship config
